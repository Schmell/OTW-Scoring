import { Fragment, h } from "preact";

interface indexProps {}

const New = ({}) => {
  return (
    <Fragment>
      <div>
        <h1>hey</h1>
      </div>
    </Fragment>
  );
};

export default New;
