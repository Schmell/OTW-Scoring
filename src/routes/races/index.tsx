import { Fragment, h } from "preact";
import { FC } from "preact/compat";

interface racesProps {}

const Races: FC<racesProps> = ({}) => {
  return (
    <Fragment>
      <h1>Races</h1>
      <div></div>
    </Fragment>
  );
};

export default Races;
